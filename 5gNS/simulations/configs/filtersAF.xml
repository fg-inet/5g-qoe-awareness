<filters>
  <filter destAddress="serverVID" gate="1"/>
  <filter destAddress="serverLVD" gate="0"/>
  <filter destAddress="serverFDO" gate="1"/>
  <filter destAddress="serverSSH" gate="0"/>
  <filter destAddress="serverVIP" gate="0"/>
  
  <filter srcAddress="serverVID" gate="1"/>
  <filter srcAddress="serverLVD" gate="0"/>
  <filter srcAddress="serverFDO" gate="1"/>
  <filter srcAddress="serverSSH" gate="0"/>
  <filter srcAddress="serverVIP" gate="0"/>
</filters>
